export interface Eventos {
    id?: number;
    nombre: string,
    slig?: string,
    descripcion?: string,
    image?: string,
    fecha_creacion?: Date,
    fecha_edicion?: Date,
    precio?: number,
    bg_color?: string;
}